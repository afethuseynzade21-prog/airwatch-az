version: "3.9"

services:
  airwatch:
    build: .
    ports:
      - "8501:8501"
      - "8000:8000"
    environment:
      - WAQI_TOKEN=${WAQI_TOKEN}
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs
    restart: unless-stopped